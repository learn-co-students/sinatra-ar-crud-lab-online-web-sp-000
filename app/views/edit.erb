<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Edit Article</title>
  </head>
  <body>
    <form action='/articles/<%= @article.id %>' method='POST'>
        <input id="hidden" type="hidden" name="_method" value="PATCH">
        <label for='title' id='title'>Edit Title:</label><input type='text' name='title' placeholder='<%= @article.title %>'></br>
        <label for='content' id='content'>Edit Content:</label></br>
        <textarea name='content' cols='25' rows='10'><%= @article.content %></textarea></br>
        <input type='submit' id='submit'>
  </form>
  </body>
</html>
