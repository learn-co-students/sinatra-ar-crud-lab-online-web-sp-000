<body style="font-size:18;">

<form method=post action='/articles/<%= @article.id %>' id="editform">
<input id="hidden" type="hidden" name="_method" value="PATCH">
<p>
    <label for="title"> Article Title</label>
    <input type="text" id="title" name="article[title]" value="<%= @article.title %>">
    <button type="submit" value="submit">Submit</button>
</p>
</form>
<p>
    <label for="content"> Content</label>
</p>
    <textarea  rows="4" cols="50" id="content" name="article[content]" form="editform"
    value="<%= @article.content %>">
    </textarea>
</body>

    
   