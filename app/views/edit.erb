<!DOCTYPE html>
<html lang='en'>
  <head>
    <meta charset="utf-8" />
    <title>EDIT</title>
  </head>
  <body>
    <form action="/articles/<%= @article.id %>" method="post">
      <input type="hidden" name="_method" value="patch">
      <p>Title: <input id="title" type="text" value="<%= @article.title %>" name="article[title]"> </p>
      <p>Content: <textarea id="content" type="text" value="<%= @article.content %>"  name="article[content]"  rows="4" cols="50">Make edits here!</textarea></p>
      <input type="submit">
    </form>
  </body>
</html>