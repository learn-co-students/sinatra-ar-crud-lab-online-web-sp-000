<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Articles</h1>
    <!-- Render articles -->
    <% if @articles.empty? %>
      <h3>There are currently no articles</h3>
    <% else %>
      <% @articles.each do |article| %>
        <p><%= article.title %> | <a href="/articles/<%= article.id %>/edit">edit</a></p>
      <% end %>
    <% end %> <br>
    <p><a href="/articles/new">Create an article</a></p>
  </body>
</html>