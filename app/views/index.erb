<!DOCTYPE html>
    <head>
    </head>

    <body>
        <ol> 
        <% @articles.each do |article| %> 
            <li><a href="/articles/<%= article.id %>"><%= article.title %></a></li> 
        <% end %>
        </ol>
    </body>

</html>